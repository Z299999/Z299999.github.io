.DS_Store
__pycache__/
*.pyc
# CV: only the compiled PDF is published. The LaTeX source, the document
# class, the build script and NOTES.md stay local -- NOTES.md and the
# hidden \iffalse block in resume.tex carry work not yet made public.
# The files are still on disk and edited as usual; git just ignores them.
cv/*
!cv/resume.pdf
